<!DOCTYPE html>
<html lang="en">
<head>
	<% include ../../partials/head %>
</head>
<body class="container">

	<header>
		<% include ../../partials/header %>
	</header>

	<main>
        These are all your saved objects
        <br>

        <div class='commands-div'>
            <a class='command-button' href='/objects/create'>
                + Add new price check object
            </a>
        </div>

        <table class='grid-table'>
            <tr>
                <th>
                    Image
                </th>
                <th>
                    Name
                </th>
                <th>
                    Old price
                </th>
                <th>
                    New price
                </th>
                <th>
                    Difference
                </th>
                <th>
                    Website
                </th>
            </tr>
            <% if (objectslist && objectslist.length > 0) { %>
                <% for(var i=0; i < objectslist.length; i++) { %>
                    <tr>
                        <td style='text-align: center;'><img src='<%= objectslist[i].imageUrl %>' alt='objectslist[i].name '/></td>
                        <td><%= objectslist[i].name %></td>
                        <td><%= objectslist[i].oldPrice %></td>
                        <td><%= objectslist[i].newPrice %></td>
                        <td><%= objectslist[i].newPrice - objectslist[i].oldPrice %></td>
                        <td><a href='<%= objectslist[i].objectUrl %>'><%= objectslist[i].website %></a></td>
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan='6'> 
                        No data
                    </td>
                </tr>
            <% } %>
        </table>
	</main>

	<footer>
		<% include ../../partials/footer %>
	</footer>
	
</body>
</html>